// js/login.js - Minimal version to avoid conflicts
console.log("Login JavaScript loaded");

// Remove any existing login form handling
// Keep only navigation functions if needed
function goBack() {
    window.history.back();
}